/******************************************************************************

                              Online C++ Compiler.
               Code, Compile, Run and Debug C++ program online.
Write your code in this editor and press "Run" button to compile and execute it.

*******************************************************************************/

#include <iostream>
#include<cassert>
// using namespace std;
template<typename T, T... N>
class propertyMethod
{
private:
    T... N;
public:
    Set
class Setter
{
private:
    int x, y;
public:
    Setter SetX(int x)
    {
        assert(is_and_required, "The `And' method is required instead of `Set'!\n");
        this->x = x;
        return *this;
    }
    int AndX(int x)
    {
        assert(is_set_required, "The `Set' method is required instead of `And'!\n");
        this->x = x;
        return x;
    }
}
int main()
{
    cout<<"Hello World";

    return 0;
}